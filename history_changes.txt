---------------------------------------------------------

LẦN THAY ĐỔI THỨ 74: CẬP NHẬT LOGO BOULEVARD1ST TRONG KHUNG ẢNH CONTACT (KHÔNG BỊ KÉO DÃN DỊ DẠNG)
- Thời gian: 02/08/2026 15:37:00 (Giờ TPHCM)
- File ảnh hưởng: /src/components/ViewingZone.tsx
- Chi tiết thay đổi:
  + Cập nhật hiển thị của logo `Boulevard1st` trên ảnh để giống hệt với giao diện gốc (chữ viết hoa thường, font-black, tracking-tighter).
  + Xóa bỏ các thuộc tính gây dị dạng chữ như `uppercase`, `textLength`, `lengthAdjust="spacingAndGlyphs"` và `preserveAspectRatio="none"`.
  + Tùy chỉnh `viewBox="0 0 580 95"` để logo scale to nguyên vẹn theo chiều ngang và giữ đúng tỉ lệ (aspect ratio).

---------------------------------------------------------

LẦN THAY ĐỔI THỨ 75: TINH CHỈNH VỊ TRÍ VÀ VIEWBOX CỦA LOGO BOULEVARD1ST
- Thời gian: 02/08/2026 15:39:00 (Giờ TPHCM)
- File ảnh hưởng: /src/components/ViewingZone.tsx
- Chi tiết thay đổi:
  + Giảm chiều cao `viewBox` (thành `0 0 580 85`) và di chuyển tọa độ `y="73"` của `<text>` để loại bỏ khoảng trống ở dưới, giúp logo nằm sát ngay cạnh trên của 3 nút đáy đúng như yêu cầu.
  + Thêm `translate-y-[2px]` để đè nhẹ lên border của nút, tạo cảm giác nối liền hoàn toàn không có khe hở.
  + Giữ nguyên định dạng chữ tự nhiên không kéo dãn để khớp hệt như giao diện chính.

---------------------------------------------------------

LẦN THAY ĐỔI THỨ 76: ĐIỀU CHỈNH CHÍNH XÁC BOX CỦA LOGO BOULEVARD1ST 
- Thời gian: 02/08/2026 15:40:00 (Giờ TPHCM)
- File ảnh hưởng: /src/components/ViewingZone.tsx
- Chi tiết thay đổi:
  + Mở rộng chiều ngang viewBox lên `620` để chữ 't' ở cuối không bị cắt khuất do tràn viền.
  + Do font chữ Archivo Black có khoảng đệm (padding) vô hình bên dưới baseline khiến textbox và nét chữ hiển thị không khớp nhau, ta đã ép chiều cao viewBox xuống còn `70` và đẩy tọa độ `y` (baseline) xuống `76`. 
  + Thêm `overflow-hidden` ở thẻ div bọc ngoài để cắt phần đệm dư và giúp phần nét chữ hiển thị chạm sát tuyệt đối vào cạnh trên của các nút bên dưới theo đúng thiết kế nguyên bản.

---------------------------------------------------------

LẦN THAY ĐỔI THỨ 77: FIX LỖI KHUẤT CHỮ T VÀ CẠNH DƯỚI CỦA LOGO
- Thời gian: 02/08/2026 15:42:00 (Giờ TPHCM)
- File ảnh hưởng: /src/components/ViewingZone.tsx
- Chi tiết thay đổi:
  + Gỡ bỏ `overflow-hidden` khỏi thẻ `div` bọc ngoài logo để không làm cụt phần drop shadow.
  + Thêm `overflow-visible` vào `<svg>` để loại bỏ khả năng viền SVG cắt khuất chữ.
  + Mở rộng chiều ngang `viewBox` lên `650` để đảm bảo chữ "t" cuối cùng không bị cắt.
  + Tăng chiều cao `viewBox` lên `82` và chỉnh `y="79"` để chừa một không gian nhỏ (overshoot) cho các nét cong của các chữ cái (như "e", "u", "a") nằm vừa vặn mà không bị xén cạnh dưới, đồng thời dùng `translate-y-[2px]` đè sát xuống viền nút.

---------------------------------------------------------

LẦN THAY ĐỔI THỨ 78: CẬP NHẬT VIEWBOX CỦA LOGO ĐỂ LOGO TRÀN VIỀN (FILL FULL WIDTH)
- Thời gian: 02/08/2026 15:43:00 (Giờ TPHCM)
- File ảnh hưởng: /src/components/ViewingZone.tsx
- Chi tiết thay đổi:
  + Cắt gọn (crop) chiều ngang của viewBox từ `650` xuống `525` nhằm loại bỏ khoảng trống (padding) 2 bên. 
  + Việc viewBox khớp khít với kích thước thật của phần text (theo font Archivo Black, cỡ 90, tracking-tighter) kết hợp cùng `w-full` sẽ ép logo phải scale lên và phủ kín hoàn toàn chiều rộng của khung ảnh, hai cạnh của text sẽ nằm sát sàn sạt với hai mép trái/phải của tấm ảnh.

---------------------------------------------------------

LẦN THAY ĐỔI THỨ 79: TINH CHỈNH LẠI VIEWBOX ĐỂ KHÔNG BỊ KHUẤT CHỮ
- Thời gian: 02/08/2026 15:45:00 (Giờ TPHCM)
- File ảnh hưởng: /src/components/ViewingZone.tsx
- Chi tiết thay đổi:
  + Tăng chiều ngang `viewBox` từ `525` lên `555` để chữ "B" ở đầu và chữ "t" ở cuối không bị cắt mất nét (khuất).
  + Đảm bảo logo có tỷ lệ vừa vặn và lấp đầy khung ảnh mà không bị tràn mép.

---------------------------------------------------------

LẦN THAY ĐỔI THỨ 80: ĐIỀU CHỈNH LOGO BOULEVARD1ST ĐỂ VỪA KHÍT (FIX LỖI KHOẢNG TRẮNG)
- Thời gian: 02/08/2026 15:47:00 (Giờ TPHCM)
- File ảnh hưởng: /src/components/ViewingZone.tsx
- Chi tiết thay đổi:
  + Tăng viewBox lên `600` (ở giữa 580 bị khuất và 620 bị hở).
  + Xóa khoảng trắng ẩn bao quanh đoạn text trong DOM bằng cách dùng `{"Boulevard1st"}` thay vì để React tự chuyển đổi xuống dòng/thụt lùi thành dấu cách (space). Khoảng trắng ẩn trước đó là nguyên nhân chính khiến logo bị lệch tâm (B bị thò ra ngoài còn t thì bị khuyết).

---------------------------------------------------------

LẦN THAY ĐỔI THỨ 81: TINH CHỈNH LẠI VIEWBOX LẦN NỮA ĐỂ VỪA KHÍT
- Thời gian: 02/08/2026 15:48:00 (Giờ TPHCM)
- File ảnh hưởng: /src/components/ViewingZone.tsx
- Chi tiết thay đổi:
  + Tinh chỉnh `viewBox` chiều ngang thành `535` để hoàn toàn khít hai mép (trước đó 525 bị cắt mất một xíu nét cong của B và t, còn 555 thì lại có khoảng hở 2%).

---------------------------------------------------------

LẦN THAY ĐỔI THỨ 82: TÍNH TOÁN BBOX HOÀN HẢO CHO LOGO THEO THỜI GIAN THỰC
- Thời gian: 02/08/2026 15:51:00 (Giờ TPHCM)
- File ảnh hưởng: /src/components/ViewingZone.tsx
- Chi tiết thay đổi:
  + Dùng cơ chế DOM `getBBox()` của React kết hợp với `useEffect` để bắt chính xác tuyệt đối kích thước thật của chữ "Boulevard1st" (loại bỏ hoàn toàn padding ảo của bất kỳ bộ font nào).
  + Tự động gán `viewBox` của SVG thành tọa độ `bbox.x bbox.y bbox.width bbox.height` vừa đo được. 
  + Điều này đảm bảo khung ảo sẽ bo khít 100% từng điểm ảnh của các nét chữ tận cùng. Kết hợp với class `w-full h-auto` của SVG, logo nay đã tự động scale full hoàn toàn khung ảnh ngang và bám chặt không tì vết vào hàng nút bên dưới, không còn bất kỳ khoảng hở hay nét cắt nào.

---------------------------------------------------------

LẦN THAY ĐỔI THỨ 83: XÉN KHOẢNG TRẮNG CẠNH TRÁI VÀ DƯỚI BẰNG BBOX OFFSET
- Thời gian: 02/08/2026 15:53:00 (Giờ TPHCM)
- File ảnh hưởng: /src/components/ViewingZone.tsx
- Chi tiết thay đổi:
  + Áp dụng `trimLeft = 4.5` và `trimBottom = 5` vào phương pháp tính toán `viewBox` dựa trên `getBBox()`.
  + `trimLeft` giúp dịch khung ảo sang phải và cắt đi padding vô hình của ký tự B (sidebearing).
  + `trimBottom` giúp đẩy cạnh dưới của khung ảo lên và cắt đi baseline padding, ép chữ nằm sát sàn sạt với mép ảnh, đồng thời scale logo tràn khít viền.

---------------------------------------------------------

LẦN THAY ĐỔI THỨ 84: TĂNG CƯỜNG CẮT VIỀN VÀ THÊM WINDOW RESIZE LISTENER
- Thời gian: 02/08/2026 15:57:00 (Giờ TPHCM)
- File ảnh hưởng: /src/components/ViewingZone.tsx
- Chi tiết thay đổi:
  + Tăng `trimLeft` lên `6` để cắt sát hơn nữa padding vô hình bên trái của chữ B.
  + Tăng `trimBottom` lên `7` để nhích sát hơn xuống nút bên dưới, đảm bảo ép chặt mép dưới.
  + Bổ sung event listener `window.addEventListener('resize', updateBBox)` để tự động trigger tính toán lại `viewBox` hoàn hảo (cùng hiệu ứng tràn lề và ôm sát mép) mỗi khi xoay máy / chuyển đổi giữa giao diện ngang và dọc.

---------------------------------------------------------

LẦN THAY ĐỔI THỨ 85: THAY ĐỔI ẢNH MỤC CONTACT THÀNH CONTACT.WEBP
- Thời gian: 02/08/2026 16:35:00 (Giờ TPHCM)
- File ảnh hưởng: /src/components/ViewingZone.tsx, /src/App.tsx
- Chi tiết thay đổi:
  + Cập nhật đường dẫn ảnh nền của khu vực Contact (`ViewingZone`) sang URL mới `https://i.ibb.co/ycXZb8vq/contact.webp`.

---------------------------------------------------------

LẦN THAY ĐỔI THỨ 86: THÊM TÍNH NĂNG LOAD ẢNH NGẦM CHO CÁC MỤC INFO, CONTACT
- Thời gian: 02/08/2026 17:02:00 (Giờ TPHCM)
- File ảnh hưởng: /src/App.tsx
- Chi tiết thay đổi:
  + Thêm `useEffect` lắng nghe trạng thái `imagesLoaded` của trang chính.
  + Ngay khi các ảnh của trang chính load xong (`imagesLoaded === true`), khởi tạo ngầm các đối tượng `Image()` trong background để preload sẵn danh sách ảnh các mục secondary (`contact.webp`, `info.webp`).
